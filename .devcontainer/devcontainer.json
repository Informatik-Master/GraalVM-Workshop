{
    "image": "ubuntu",
    "forwardPorts": [3000, 8080],
    "remoteEnv": {
        "JAVA_HOME": "/workspaces/graal-workshop/graalvm-ce-java11-22.3.1",
        "PATH": "/workspaces/graal-workshop/graalvm-ce-java11-22.3.1/bin:${localEnv:PATH}",
        "GRAALVM_HOME": "/workspaces/graal-workshop/graalvm-ce-java11-22.3.1"
    },
    "customizations": {
    "vscode": {
      "extensions": ["dbaeumer.vscode-eslint"]
    }
  },
  "postCreateCommand": "apt-get update && apt-get install -y git unzip build-essential libz-dev zlib1g-dev && cd /workspaces/graal-workshop && wget https://github.com/graalvm/graalvm-ce-builds/releases/download/vm-22.3.1/graalvm-ce-java11-linux-amd64-22.3.1.tar.gz && tar -xvzf graalvm-ce-java11-linux-amd64-22.3.1.tar.gz && rm graalvm-ce-java11-linux-amd64-22.3.1.tar.gz"
}